export function RevenuePanel() {
  return (
    <section className="panel" aria-labelledby="revenue-deferred-heading">
      <h2 id="revenue-deferred-heading">3. Revenue &amp; curator rewards</h2>
      <p className="muted">
        The per-scene <strong>StakeToCurate</strong> MVP includes stake, rank, unstake, and
        authority rank reset only. <code>deposit_revenue</code> / curator distribution
        across many scenes is deferred to a follow-up milestone.
      </p>
    </section>
  )
}
